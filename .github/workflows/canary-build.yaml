name: Canary Build
on:
  workflow_dispatch:
  push:
    branches: [ "master" ]
  schedule:
    - cron: '30 18 * * *' # Everyday, at 00:00 IST


jobs:
  update-flake-lock:
    name: Update `flake.lock`
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: DeterminateSystems/nix-installer-action@main
      - run: nix flake update
      - uses: actions/upload-artifact@main
        with:
          name: flake-lock
          path: flake.lock
          retention-days: 1
          overwrite: true
          if-no-files-found: error
          compression-level: 9

  canary-build:
    needs: update-flake-lock
    name: Building ${{ matrix.nixTarget }} for ${{ matrix.nixSystem }}
    runs-on: ${{ matrix.nixSystem == 'x86_64-linux' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    strategy:
      fail-fast: false
      matrix:
        nixTarget: [ nixosConfigurations, homeConfigurations, devShells, packages, ]
        nixSystem: [ aarch64-linux, x86_64-linux, ]
    steps:
      - uses: actions/checkout@main

      - name: Provide AWS Secrets to nix-daemon
        run: |
          sudo mkdir -vp /root/.aws
          sudo tee /root/.aws/credentials << EOF
          [default]
          aws_access_key_id = ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key = ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          EOF

      - uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: "substituters = s3://thefossguy-nix-cache-001-8c0d989b-44cf-4977-9446-1bf1602f0088?region=us-east-1\nextra-trusted-public-keys = 10.0.0.24:g29fjBRU/VGj6kkIQqjm0o5sxWduZ1hNNLTnSeF/AAU="

        # Almost all of the jobs fail and I suspect it is because of the rate
        # limit that's on Linode's side. Add this cache to cushion the impact
        # of ~8 jobs querying the cache almost at once.
      - uses: DeterminateSystems/magic-nix-cache-action@main

      - uses: actions/download-artifact@main
        with:
          name: flake-lock
          path: .

      - name: Testing out the binary cache
        run: time nix run .#miscPackages.${{ matrix.nixSystem }}.binfmtCheck
        timeout-minutes: 2

      - name: Running CI
        run: python3 ./scripts/nix-ci/builder.py --${{ matrix.nixTarget }}
        timeout-minutes: 10

      - name: Look at results
        run: ls -alh | grep result || echo 'Nothing was built'

  final-check:
    needs: canary-build
    name: Final check
    runs-on: ubuntu-latest
    steps:
      - run: echo 'All jobs build succeeded.'
